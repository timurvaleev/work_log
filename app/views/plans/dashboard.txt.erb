Actual Log (skype)

<% @plans.each do |plan| -%>
<%= "Extra Hours:\n#{plan.extra_hours}\n\n" if plan.extra_hours.present? -%>
<%= format_date(plan.date_for) %>
<%= plan.actual_log %>
<% end -%>

----------------------------------------------------

Report Log (campfire)

<% @plans.each do |plan| -%>
<%= "Extra Hours:\n#{plan.extra_hours}\n\n" if plan.extra_hours.present? -%>
<%= format_date(plan.date_for) %>
<%= plan.report_log %>
<% end -%>
